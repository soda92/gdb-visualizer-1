{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/gdbviz1.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            // "preLaunchTask": "build",
            "initCommands": [
                "command source ${workspaceFolder}/.lldbinit"
            ]
        }
    ]
}